export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-3xl font-semibold">OpenVerdict</h1>
      <p className="max-w-xl text-center text-sm opacity-70">
        Decentralized intelligence verification engine. Frontend under
        construction — the engine and CLI are the product; this observer is a
        read-only projection.
      </p>
    </main>
  );
}
