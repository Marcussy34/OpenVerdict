---
title: Public API
description: Every route under /api, with its limits and status codes. Rendered from docs/API.md in the repository.
order: 5
source: docs/API.md
---

This page renders `docs/API.md` from the repository at request time, so the
reference here and the reference in the source tree are the same file. Links
that point into the repository open on GitHub.

Base URL of the production deployment: `https://app.openverdict.info/api`.
